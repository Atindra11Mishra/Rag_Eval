fastapi==0.115.14
uvicorn[standard]==0.35.0
python-multipart==0.0.20
pydantic==2.11.7
pydantic-settings==2.10.1
python-dotenv==1.1.1

# Python 3.13 needs NumPy 2.x wheels. Older LangChain 0.3.x pinned numpy<2
# transitively and forced a local NumPy source build on Windows.
numpy==2.3.1

# Document parsing
pymupdf==1.26.3

# LangChain + ChromaDB
langchain==1.3.4
langchain-openai==1.2.2
langchain-community==0.4.2
langchain-chroma==1.1.0
chromadb==1.5.9

# Lexical retrieval (Phase 2, installed now for completeness)
rank-bm25==0.2.2

# Reranker (Phase 4, installed now)
sentence-transformers==5.5.1

# OpenAI
openai>=2.26.0,<3.0.0

# Evaluation (Phase 5)
ragas==0.4.3
datasets==4.1.1

# Supabase (Phase 7)
supabase==2.18.1
